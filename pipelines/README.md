# Lakeflow declarative pipelines

Lakeflow pipeline definitions (YAML) that transform Bronze ingestion → Silver normalized tables → Gold serving tables.

Includes data quality expectations (nulls, dupes) and uses `ai_query`/`ai_extract` for unstructured label text structuring.

Phase 1 deliverable. See ARCHITECTURE.md §7.
